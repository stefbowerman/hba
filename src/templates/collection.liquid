{%- comment -%} Overload the collection filtering functionality so we can use the /collections/{{ handle }}/lookbook url {%- endcomment -%}
{% if current_tags contains 'lookbook' %}
  {% section 'lookbook' %}
{% else %}
  {% section 'collection' %}
{% endif %}
